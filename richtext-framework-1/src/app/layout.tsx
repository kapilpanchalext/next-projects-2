import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "RichText Editor Framework - 1",
  description: "Generated by RichText Editor Framework - 1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
